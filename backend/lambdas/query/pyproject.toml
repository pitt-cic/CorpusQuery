[project]
name = "query-lambda"
version = "0.1.0"
description = "CorpusQuery Query Lambda handler"
requires-python = ">=3.12"
dependencies = [
    "aws-lambda-powertools>=3.0.0",
    "boto3>=1.40.0",
    "litellm>=1.87.0",
    "paper-qa>=2026.3.18",
    "vectorstore",
]

[tool.uv.sources]
vectorstore = { workspace = true, editable = true }
