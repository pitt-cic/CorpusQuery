[project]
name = "api-lambda"
version = "0.1.1"
description = "CorpusQuery API Lambda handler"
requires-python = ">=3.12"
dependencies = [
    "aws-lambda-powertools>=3.0.0",
    "boto3>=1.40.0",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true, editable = true }
